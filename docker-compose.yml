version: '3.8'

services:
  gsongrok:
    build: .
    image: ramosisw/gsongrok
    container_name: gsongrok
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - APIKEY=${APIKEY}
      - HOST=${HOST}
    ports:
      - "8080:8080"
    restart: always
